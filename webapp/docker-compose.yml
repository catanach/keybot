# Updated 2026-09-02 19:30 - Force rebuild with cache buster
services:
  keybot-web:
    build:
      context: .
      args:
        BUILD_TIMESTAMP: "$(date +%s)"
    ports:
      - "8000:8000"
      - "5000:8000"
    volumes:
      - ./app:/app/app
      - ./data:/data
      - ../src:/firmware_src:ro
      - ./app:/app/app
    restart: unless-stopped
